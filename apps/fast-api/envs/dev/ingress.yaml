apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fast-api-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: dev.eodatahub.org.uk/stac-api
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: stac-fast-api-service
                port:
                  number: 8080
  tls:
    - hosts:
        - dev.eodatahub.org.uk
      secretName: dev.eodatahub.org.uk-tls
